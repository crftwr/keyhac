theme: jekyll-theme-primer
title: "Keyhac"
description: "Python-scriptable keyboard customization for Windows and macOS"

# doc/config-py-template.md renders the config template (keyhac/_config.py) as a
# syntax-highlighted page. include_relative cannot reach ../keyhac from doc/,
# so the package directory doubles as the Liquid includes dir. (The site uses
# no other includes.)
includes_dir: keyhac
